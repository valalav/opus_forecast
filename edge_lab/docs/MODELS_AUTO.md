# Sirena Models - Auto-Generated Documentation

Generated by: scripts/doc_gen.py
Total Models: 37

---

## AR1Forecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/arima.py:163-239`

### Description

```
Простая AR(1) модель.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `kwargs` | Any | None | |

---

## BVARForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/bvar.py:25-816`

### Description

```
Полная Байесовская VAR с Minnesota Prior и Inverse-Wishart на Σ.

Гиперпараметры Minnesota Prior:
- lambda1: Overall tightness (по умолчанию 0.2, или auto)
- lambda2: Cross-variable tightness (по умолчанию 0.5)
- lambda3: Lag decay (по умолчанию 1.0)
- lambda4: Intercept prior variance (по умолчанию 100)

Гиперпараметры Inverse-Wishart:
- sigma_prior_df: Степени свободы (по умолчанию k+2)
- sigma_prior_scale: Масштаб (по умолчанию 1.0)

Автовыбор:
- auto_lambda: Empirical Bayes для lambda1
- auto_lags: Выбор лагов по BIC
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `lags` | int | 1 | |
| `lambda1` | float | 0.2 | |
| `lambda2` | float | 0.5 | |
| `lambda3` | float | 1.0 | |
| `lambda4` | float | 100 | |
| `n_draws` | int | 1000 | |
| `sigma_prior_df` | Optional[int] | required | |
| `sigma_prior_scale` | float | 1.0 | |
| `auto_lambda` | bool | False | |
| `auto_lags` | bool | False | |
| `max_lags` | int | 4 | |
| `use_gibbs` | bool | False | |
| `gibbs_iter` | int | 1000 | |
| `gibbs_burnin` | int | 200 | |
| `var_names` | List[str] | required | |

---

## BVARRateForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/bvar_rate.py:29-664`

### Description

```
BVAR модель с эндогенной ключевой ставкой для сценарного анализа.

Позволяет прогнозировать инфляцию при разных траекториях ставки.

Переменные (по умолчанию):
- CPI total
- CPI food (продовольствие)
- CPI nonfood (непродовольствие)
- CPI services (услуги)
- Ki (ключевая ставка)
- Ruonia

Сценарии ставки:
- 'base': текущий уровень Ki
- 'hike': повышение на 2 п.п. за 6 месяцев
- 'cut': снижение на 2 п.п. за 6 месяцев
- array[horizon]: пользовательская траектория Ki
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `lags` | int | 2 | |
| `lambda1` | float | 0.2 | |
| `lambda2` | float | 0.5 | |
| `lambda3` | float | 1.0 | |
| `lambda4` | float | 100 | |
| `n_draws` | int | 500 | |
| `include_ruonia` | bool | True | |
| `include_usd` | bool | False | |

---

## BayesianRidgeForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/bayesian_ridge.py:29-449`

### Description

```
Байесовская Ridge регрессия с автоматической регуляризацией.

Возвращает точечный прогноз и доверительные интервалы.

Параметры:
    alpha_1, alpha_2: Prior параметры для alpha (шум)
    lambda_1, lambda_2: Prior параметры для lambda (регуляризация)
    n_iter: Максимальное число итераций

Выходы:
    - forecast(): точечный прогноз (как Ridge)
    - forecast_with_ci(): прогноз + стандартное отклонение
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha_1` | float | 1e-06 | |
| `alpha_2` | float | 1e-06 | |
| `lambda_1` | float | 1e-06 | |
| `lambda_2` | float | 1e-06 | |
| `n_iter` | int | 300 | |
| `use_macro` | bool | True | |

---

## BudgetRidgeForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/budget_ridge.py:26-411`

### Description

```
Ridge регрессия с бюджетными признаками КБР.

Особенности:
- Использует бюджетные доходы КБР с лагом 3 месяца
- Комбинирует с базовыми макро-признаками
- Исключение выбросных лет (2022, 2010)

Бюджетные признаки (lag 3):
- budget_income_L3: Доходы всего (r=+0.484)
- budget_ndfl_L3: НДФЛ (r=+0.466)
- budget_profit_L3: Налог на прибыль (r=+0.427)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | 1.0 | |
| `use_huber` | bool | False | |
| `budget_data_path` | Optional[str] | required | |

---

## CatBoostForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/catboost_model.py:37-351`

### Description

```
CatBoost для прогнозирования инфляции.

Особенности:
- Ordered boosting (меньше переобучение)
- Категориальный признак месяца
- Агрессивная регуляризация для малых выборок
- Ранняя остановка

Parameters:
    iterations: Количество деревьев (default: 200)
    depth: Глубина дерева (default: 4, низкая для регуляризации)
    learning_rate: Шаг обучения (default: 0.05)
    l2_leaf_reg: L2 регуляризация (default: 5.0)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `iterations` | int | 200 | |
| `depth` | int | 4 | |
| `learning_rate` | float | 0.05 | |
| `l2_leaf_reg` | float | 5.0 | |
| `random_seed` | int | 42 | |

---

## ConformalForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/conformal.py:24-373`

### Description

```
Ridge с Conformal Prediction для калиброванных CI.

Гарантирует заданный coverage rate (например, 90%).
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | required | |
| `coverage_target` | float | required | |
| `calibration_ratio` | float | required | |
| `quantile_multiplier` | float | required | |

---

## EBMForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ebm.py:37-477`

### Description

```
Explainable Boosting Machine для прогнозирования инфляции.

Архитектура: GAM (Generalized Additive Model) с boosting
Интерпретируемость: Полная — графики влияния каждого признака

Особенности:
- Аддитивная структура: сумма вкладов признаков
- Автоматические доверительные интервалы
- Метод explain() для интерпретации
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `max_bins` | int | 256 | |
| `max_interaction_bins` | int | 32 | |
| `interactions` | int | 0 | |
| `outer_bags` | int | 8 | |
| `inner_bags` | int | 0 | |
| `learning_rate` | float | 0.01 | |
| `min_samples_leaf` | int | 2 | |
| `max_leaves` | int | 3 | |

---

## ETSForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ets.py:22-179`

### Description

```
ETS (Error-Trend-Seasonality) модель.

Использует Holt-Winters экспоненциальное сглаживание.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `trend` | str | 'add' | |
| `seasonal` | str | 'add' | |
| `seasonal_periods` | int | 12 | |
| `damped_trend` | bool | False | |

---

## ElasticNetForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/elasticnet.py:28-379`

### Description

```
ElasticNet с автоматическим подбором гиперпараметров.

Использует ElasticNetCV для выбора оптимального alpha и l1_ratio
через кросс-валидацию.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `l1_ratios` | List[float] | required | |
| `alphas` | List[float] | required | |
| `cv` | int | 5 | |
| `use_macro` | bool | True | |

---

## ExogForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/exog_forecaster.py:41-486`

### Description

```
Прогноз экзогенных переменных для инфляционных моделей.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `ki_method` | Literal['naive', 'ar1', 'adaptive', 'taylor'] | 'adaptive' | |
| `usd_method` | Literal['naive', 'ar1', 'adaptive'] | 'adaptive' | |
| `brent_method` | Literal['naive', 'ar1', 'futures'] | 'ar1' | |
| `current_usd_abs` | Optional[float] | required | |
| `current_ki` | Optional[float] | required | |
| `current_ruonia` | Optional[float] | required | |
| `current_brent` | Optional[float] | required | |

---

## ExogProphetForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/exog_prophet.py:40-625`

### Description

```
Prophet с экзогенными переменными.

Оптимизирован для h=12 (годовая траектория).
Использует лаги экзогенных из тестов Грейнджера.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `use_usd` | bool | True | |
| `use_brent` | bool | True | |
| `use_ki` | bool | True | |
| `yearly_seasonality` | bool | True | |
| `seasonality_mode` | str | 'additive' | |
| `changepoint_prior_scale` | float | 0.01 | |
| `seasonality_prior_scale` | float | 1.0 | |
| `outlier_years` | List[int] | required | |

---

## HierarchicalMicroForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/hierarchical_micro.py:46-666`

### Description

```
Hierarchical bottom-up forecaster: Micro → Subcomp → Component → Total

Parameters
----------
region_code : int
    Region code (7 = КБР, default)
horizon : int
    Forecast horizon (1, 2, or 12)
train_start : str
    Start date for training
use_prophet_for_services : bool
    Use Prophet for seasonal services (ЖКХ, образование)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `region_code` | Any | 7 | |
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `use_prophet_for_services` | Any | True | |
| `random_state` | Any | 42 | |

---

## HorizonEnsembleForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/horizon_ensemble.py:30-206`

### Description

```
Adaptive ensemble: Huber + Micro with horizon-dependent weights.

Short horizons: Huber dominates (autocorrelation is strong)
Long horizons: Micro dominates (structural info matters more)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |

---

## HuberForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/huber.py:29-377`

### Description

```
Huber Regressor — робастная к выбросам модель.

Автоматически понижает влияние выбросов (2022 и др.)
без необходимости исключать их из обучения.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `epsilon` | float | 1.35 | |
| `alpha` | float | 0.3 | |
| `max_iter` | int | 500 | |
| `use_macro` | bool | True | |

---

## KiTrajectoryForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ki_trajectory.py:34-293`

### Description

```
Эндогенное моделирование ключевой ставки по правилу Тейлора.

Правило ЦБ РФ (упрощённое):
    Ki_target = neutral_rate + phi_inf * (inf_yoy - inf_target)
    Ki_t+1 = inertia * Ki_t + (1 - inertia) * Ki_target

Параметры калибруются на исторических данных или задаются вручную.

Attributes:
    params: TaylorRuleParams с параметрами модели
    _is_fitted: Флаг обученности модели
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `params` | Optional[TaylorRuleParams] | required | |

---

## LightGBMForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/lightgbm.py:30-366`

### Description

```
LightGBM модель прогнозирования.

Использует базовые признаки + макро-признаки Ki/Ruonia (v4.0).
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `n_estimators` | int | 100 | |
| `max_depth` | int | 5 | |
| `learning_rate` | float | 0.1 | |
| `num_leaves` | int | 31 | |
| `min_child_samples` | int | 10 | |
| `outlier_years` | List[int] | required | |
| `use_macro` | bool | False | |

---

## MIDASForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/midas.py:38-506`

### Description

```
MIDAS (Mixed Data Sampling) Forecaster.

Combines monthly inflation with high-frequency predictors using
polynomial weighting functions.

Supported weight types:
- 'almon': Polynomial lag structure
- 'exp': Exponential decay weights
- 'beta': Beta function weights
- 'normalized_exp': Normalized exponential (NED)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `weight_type` | str | 'almon' | |
| `poly_order` | int | 2 | |
| `hf_features` | Optional[List[str]] | required | |
| `alpha` | float | 0.1 | |

---

## MicroARIMAForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/micro_arima.py:18-155`

### Description

```
Загрузчик прогнозов из micro_test.csv.

Это внешняя модель пользователя (микрокомпонентная ARIMA).
Прогнозы уже готовы, нужно только извлечь для заданного горизонта.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | int | 1 | |
| `file_path` | str | 'micro_test.csv' | |

---

## MicroOptimizedForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/micro_optimized.py:29-391`

### Description

```
Optimized microcomponent forecaster with category-specific models.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |
| `use_subcomp_for_ultra_volatile` | Any | True | |

---

## MicroPlodovoshchiForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/micro_plodovoshchi.py:54-387`

### Description

```
Forecaster for Fruits & Vegetables (субкомп. 33) using 21 microcomponents.

Key features:
- Uses USD as exogenous variable for imported items
- Applies NGBoost for volatile items (капуста, картофель, огурцы)
- Ridge for stable items
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |

---

## MicrocomponentForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/microcomponent.py:29-440`

### Description

```
Bottom-up forecaster using 537 microcomponents.

Each microcomponent is forecasted individually with:
- Ridge regression for top-100 by weight
- VotingRegressor (Ridge+Lasso) for others
- Extended features for volatile items (vegetables)
- Seasonal adjustment based on historical aggregate patterns
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |
| `top_n` | Any | 100 | |
| `use_extended_for_volatile` | Any | True | |
| `use_seasonal_adj` | Any | True | |

---

## NGBoostForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ngboost_model.py:30-384`

### Description

```
NGBoost с вероятностными прогнозами.

Возвращает:
- mean: среднее прогноза
- std: стандартное отклонение
- ci_lower, ci_upper: 90% доверительный интервал
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `n_estimators` | int | required | |
| `learning_rate` | float | required | |
| `minibatch_frac` | float | required | |

---

## NGBoostShockForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ngboost_shock.py:26-339`

### Description

```
NGBoost с shock dummies.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `n_estimators` | int | required | |
| `learning_rate` | float | required | |

---

## ProphetForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/prophet.py:31-223`

### Description

```
Prophet модель прогнозирования.

Автоматическое определение тренда и сезонности.
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `yearly_seasonality` | bool | True | |
| `weekly_seasonality` | bool | False | |
| `daily_seasonality` | bool | False | |
| `seasonality_mode` | str | 'additive' | |
| `changepoint_prior_scale` | float | 0.05 | |
| `seasonality_prior_scale` | float | 10.0 | |
| `outlier_years` | List[int] | required | |

---

## RidgeExtendedForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ridge_extended.py:26-369`

### Description

```
Расширенная Ridge регрессия с дополнительными признаками.

Новые признаки:
- y_lag3, y_lag6: средние лаги
- d_y_lag1: momentum (изменение за месяц)
- d_y_lag3: momentum (изменение за квартал)
- y_vol3, y_vol6: rolling volatility
- is_jan, is_dec: индикаторы января/декабря
- quarter_sin, quarter_cos: квартальная сезонность
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | required | |
| `use_macro` | bool | True | |

---

## RidgeForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ridge.py:26-408`

### Description

```
Ridge регрессия с сезонной ETS компонентой и макро-признаками.

Особенности:
- Лаговые признаки (1, 2, 12 месяцев)
- Сезонные синус/косинус признаки
- Комбинация с ETS по месячным весам
- Исключение выбросных лет (2022, 2010)
- Макро-признаки Ki и Ruonia (v4.0)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | required | |
| `use_macro` | bool | True | |
| `ets_weights` | Dict[int, float] | required | |

---

## RidgeMacroForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ridge_macro.py:33-463`

### Description

```
Ridge регрессия с оптимальными макро-признаками и прогнозом экзогенных.

Особенности:
- Лаги определены по тестам Грейнджера
- Использует USD lag 2, Ki lag 6, Brent lag 5
- Ruonia через первую разность (D1)
- Компоненты prod и serv
- Исключение выбросных лет (2022, 2010)
- Прогнозирование экзогенных переменных для будущих периодов
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | 1.0 | |
| `use_huber` | bool | False | |

---

## RidgeShockDummiesForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/ridge_shock_dummies.py:27-383`

### Description

```
Ridge регрессия с dummy-переменными для шоков.

Особенности:
- НЕ исключаем выбросные годы (в отличие от базового Ridge)
- Добавляем dummy-переменные для конкретных шоковых месяцев
- Модель учится на шоках через коэффициенты dummy
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `alpha` | float | required | |
| `use_macro` | bool | True | |
| `use_2022_dummy` | bool | True | |

---

## SARIMAForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/arima.py:23-160`

### Description

```
SARIMA модель прогнозирования.

Параметры:
- order: (p, d, q) для ARIMA
- seasonal_order: (P, D, Q, s) для сезонности
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `order` | Tuple[int, int, int] | (1, 0, 1) | |
| `seasonal_order` | Tuple[int, int, int, int] | (1, 0, 1, 12) | |

---

## ScenarioRateModel

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/scenario_rate.py:85-817`

### Description

```
Сценарная модель влияния ставки на инфляцию v2.0.

Использует калиброванные коэффициенты трансмиссии вместо
эконометрической оценки (которая даёт неверный знак из-за
эндогенности).

Формула:
    ΔInflation_t = Σ(β_s × ΔKi_{t-s})

где β_s — импульсная функция реакции (IRF) на шок ставки.

v2.0: Поддержка асимметричного IRF, калибровки на данных, SVAR.

Примеры:
    >>> model = ScenarioRateModel(use_asymmetric=True)
    >>> model.fit(df)
    >>>
    >>> # Сценарий повышения ставки на 2 п.п.
    >>> result = model.forecast_scenario(horizon=12, ki_change=+2.0)
    >>> print(f"Эффект на инфляцию: {result['cumulative_effect']:.2f}%")
    >>> # Эффект на инфляцию: -1.20% (асимметрично!)
    >>>
    >>> # Сценарий снижения ставки на 2 п.п.
    >>> result = model.forecast_scenario(horizon=12, ki_change=-2.0)
    >>> print(f"Эффект на инфляцию: {result['cumulative_effect']:.2f}%")
    >>> # Эффект на инфляцию: +0.60% (слабее!)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `params` | TransmissionParams | required | |
| `asymmetric_params` | AsymmetricParams | required | |
| `use_asymmetric` | bool | False | |
| `use_nonlinearity` | bool | False | |

---

## SubcomponentForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/subcomponent.py:72-337`

### Description

```
Bottom-up forecaster using 45 subcomponents with optimal approaches.

Usage:
    model = SubcomponentForecaster(horizon=12)
    model.fit(df)
    forecast = model.forecast()  # Returns aggregated MoM forecast
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 12 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |

---

## SubcomponentMultiForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/subcomponent_multi.py:188-935`

### Description

```
Multi-model bottom-up forecaster using optimal model for each subcomponent.

Models used:
- Ridge: stable goods (58% weight)
- Prophet: services with seasonality (17% weight)
- NGBoost: volatile goods (6% weight)
- VotingRegressor: baseline for others (19% weight)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | Any | 1 | |
| `train_start` | Any | '2016-01-01' | |
| `random_state` | Any | 42 | |
| `use_exog_forecast` | Any | True | |

---

## SubcomponentScenarioForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/subcomponent_scenario.py:136-655`

### Description

```
Сценарная модель с субкомпонент-специфичной чувствительностью к ставке.

Объединяет:
1. SubcomponentMultiForecaster для базового прогноза
2. Субкомпонент-специфичные IRF для эффекта ставки

Примеры:
    >>> model = SubcomponentScenarioForecaster()
    >>> model.fit(df)
    >>>
    >>> # Сценарий повышения ставки на 2 п.п.
    >>> result = model.forecast_scenario(horizon=12, ki_change=+2.0)
    >>> print(f"Эффект на CPI: {result['total_effect']:.2f}%")
    >>>
    >>> # Декомпозиция по группам
    >>> decomp = result['group_decomposition']
    >>> for group, effect in decomp.items():
    >>>     print(f"{group}: {effect:.3f}%")
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | int | 1 | |
| `train_start` | str | '2016-01-01' | |
| `random_state` | int | 42 | |
| `use_asymmetric` | bool | False | |
| `use_calibrated` | bool | True | |

---

## TemporalFusionForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/tft.py:41-606`

### Description

```
Temporal Fusion Transformer Forecaster (Simplified).

This is a lightweight TFT implementation suitable for small datasets
(inflation forecasting with ~180 monthly observations).

Key components:
1. **Temporal Features:** Lagged values, momentum, seasonality
2. **Static Covariates:** Month, calendar features
3. **Variable Selection:** Via MLP feature importance
4. **Multi-horizon Output:** Via iterative forecasting

The model uses MLPRegressor with attention-like feature importance
to simulate the variable selection and attention mechanisms of TFT.

Args:
    hidden_layers: Number of hidden layers in MLP
    hidden_size: Size of hidden layers
    learning_rate_init: Learning rate for MLP
    max_iter: Maximum iterations for training
    alpha: L2 regularization parameter
    activation: Activation function ('relu', 'tanh', 'logistic')
    solver: Solver for weight optimization ('adam', 'lbfgs')
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `hidden_layers` | int | 2 | |
| `hidden_size` | int | 64 | |
| `learning_rate_init` | float | 0.001 | |
| `max_iter` | int | 500 | |
| `alpha` | float | 0.001 | |
| `activation` | str | 'relu' | |
| `solver` | str | 'adam' | |

---

## UnifiedSubcomponentForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/unified_subcomp.py:36-375`

### Description

```
Унифицированная субкомпонентная модель v3.0.

Объединяет SubcomponentMultiForecaster (baseline) и SubcomponentScenarioForecaster (IRF).

Преимущества:
- Единый интерфейс для прогноза и сценариев
- IRF-свёртка вместо линейных rate-признаков
- Интеграция с KiTrajectoryForecaster для эндогенной ставки

Attributes:
    base_model: SubcomponentMultiForecaster для baseline
    scenario_model: SubcomponentScenarioForecaster для IRF
    ki_model: KiTrajectoryForecaster для эндогенной ставки (опционально)
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `horizon` | int | 1 | |
| `use_irf_convolution` | bool | True | |
| `use_calibrated_irf` | bool | True | |
| `use_ki_trajectory` | bool | False | |

---

## XGBoostForecaster

**Source:** `/home/valalav/_projects/sirena-kbr/sirena/models/xgboost_model.py:28-333`

### Description

```
XGBoost с оптимизацией для малых данных.

Параметры подобраны консервативно:
- max_depth=3 (не глубокие деревья)
- n_estimators=150 с early_stopping
- reg_alpha, reg_lambda для регуляризации
```

### Parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `n_estimators` | int | required | |
| `max_depth` | int | required | |
| `learning_rate` | float | required | |
| `reg_alpha` | float | required | |
| `reg_lambda` | float | required | |

---
