# Форматы данных для модели прогнозирования инфляции

Для работы модели требуется заменить тестовые файлы в папке `data/` на ваши реальные данные.

## 1. Основной файл инфляции (`data/infl_kbr.csv`)

Содержит историю месячной инфляции (MoM) по категориям.

**Параметры файла:**
*   **Кодировка:** UTF-8 (или UTF-8-SIG)
*   **Разделитель столбцов:** Точка с запятой `;`
*   **Разделитель дроби:** Запятая `,` (например: `100,5`)

**Обязательные столбцы:**

| Название столбца | Описание | Пример значения |
| :--- | :--- | :--- |
| `Товар` | Название категории. **Строго должны присутствовать 5 категорий** (см. ниже) | `Все товары и услуги` |
| `Day` | Дата (первое число месяца) | `01.01.2024` |
| `MoM` | Индекс к предыдущему месяцу (100 = 0%, 101.5 = +1.5%) | `100,45` |

**Обязательные категории в столбце `Товар`:**
1.  `Все товары и услуги` (Целевая переменная)
2.  `Продовольственные товары`
3.  `Непродовольственные товары`
4.  `Услуги`
5.  `Плодоовощная продукция, включая картофель`

**Пример содержимого:**
```csv
Товар;Day;MoM;Weight_vertical
Все товары и услуги;01.01.2020;100,45;100
Продовольственные товары;01.01.2020;101,20;38
...
```

---

## 2. Файл внешних факторов (`data/inflation_data.csv`)

Содержит макроэкономические показатели.

**Параметры файла:**
*   **Кодировка:** UTF-8 (или UTF-8-SIG)
*   **Разделитель столбцов:** Точка с запятой `;`
*   **Разделитель дроби:** Запятая `,`

**Обязательные столбцы:**

| Название столбца | Описание | Пример значения |
| :--- | :--- | :--- |
| `Date` | Дата (первое число месяца) | `01.01.2020` |
| `usd_nom_i` | Индекс курса доллара к рублю (MoM). 100 = курс не изменился, 105 = доллар вырос на 5%. | `101,2` |
| `Ruonia` | Ставка RUONIA (или Ключевая ставка ЦБ) в % годовых. | `16,5` |

**Пример содержимого:**
```csv
Date;usd_nom_i;Ruonia
01.01.2020;100,5;6,25
01.02.2020;99,8;6,00
...
```

## 3. Файл недельных цен (`data/weekly_prices.csv`)

Используется для оперативной корректировки прогноза на текущий месяц (Nowcasting).
*Исходное название: Недельные цены_абс_годы.csv*

**Параметры файла:**
*   **Кодировка:** UTF-8 (или UTF-8-SIG)
*   **Разделитель столбцов:** Точка с запятой `;`
*   **Разделитель дроби:** Запятая `,`

**Обязательные столбцы:**

| Название столбца | Описание | Пример значения |
| :--- | :--- | :--- |
| `Товары` | Название товара | `Говядина (кроме бескостного мяса), кг` |
| `Сведено` | Неделя в формате `Год_НомерНедели` | `2021_01` |
| `Значение` | Цена в рублях | `350,26` |
| `Rostat_code` | Код Росстата (не используется моделью, но может присутствовать) | `111` |

**Пример содержимого:**
```csv
Товары;Сведено;Значение;Rostat_code
Говядина (кроме бескостного мяса), кг;2021_01;350,26;111
Картофель, кг;2021_01;45,50;101
...
```

## 4. Настройка шоков

В файле `kbr_forecast.py` (внизу, в секции `if __name__ == "__main__":`) вы можете вручную указать ожидаемые будущие шоки:

```python
shocks = {
    # Формат: pd.Timestamp('YYYY-MM-DD'): величина_в_процентах
    
    # Пример: Рост тарифов ЖКХ на 9.6% в июле дает вклад в общую инфляцию
    # (9.6 * вес_жкх ~0.1) = +0.96 п.п. к индексу
    pd.Timestamp('2026-07-01'): 0.96, 
    
    # Пример: Повышение НДС или акцизов
    pd.Timestamp('2026-01-01'): 0.30, 
}
```
